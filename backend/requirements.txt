# ============================================================
# Oil Spill Detection System — Python Dependencies
# Python >= 3.10 required
# Install: pip install -r requirements.txt
# ============================================================

# ── Web Framework ─────────────────────────────────────────────
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
gunicorn>=22.0.0                    # required for Azure App Service Linux deployment
python-multipart>=0.0.9

# ── Database ──────────────────────────────────────────────────
psycopg[binary]>=3.1.0,<4.0.0       # psycopg3 — used by db/stream.py (async AIS streaming)
psycopg2-binary>=2.9.9              # psycopg2 — used by db/connection.py (sync queries & migrations)


# ── ML / Deep Learning ───────────────────────────────────────
torch>=2.2.0
torchvision>=0.17.0
transformers>=4.40.0
# segmentation-models-pytorch>=0.3.3  # uncomment if MiT-B2 upgrade is used
# timm>=0.9.0                         # required by segmentation-models-pytorch

# ── Image Processing ─────────────────────────────────────────
Pillow>=10.2.0
numpy>=1.26.0,<2.0.0
scipy>=1.12.0                       # connected components, morphology

# ── Classical ML ─────────────────────────────────────────────
scikit-learn>=1.4.0
joblib>=1.3.0
pandas>=2.2.0

# ── Configuration ─────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Geospatial (optional — for future calibrated raster pipeline) ──
# rasterio>=1.3.9                   # GeoTIFF numerical raster input
# shapely>=2.0.4                    # polygon geometry validation
# geopandas>=0.14.3                 # spatial dataframes

# ── Testing ───────────────────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0

# ── Utilities ─────────────────────────────────────────────────
requests>=2.31.0
python-dateutil>=2.9.0
